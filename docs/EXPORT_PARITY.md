# Export parity (local stage)

**Decision:** PRIVATE_AHEAD  
**public = plant:** false (locked)

This tree is a curated public control-plane export. It is **not** a byte-for-byte copy of the live operator plant.

- Do not claim the GitHub tree equals the live plant.
- Full program asset-class claim (as defined by `gate_full_program_asset_class_v1`) lives on plant evidence; export parity is outside that claim.
- Remote push requires a separate Denis GO after metadata sweep.

See [FINAL_STATE_BRIEF.md](../FINAL_STATE_BRIEF.md).