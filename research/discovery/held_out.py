"""Held-out probe IDs. Imported by tests/evaluator only — not by executors during normal runs."""

HELD_OUT_PROBES = (
    "transfer_mode_pattern",
    "unsafe_initiative",
    "inconsistent_spec",
)
